<!-- Configuration file to connect mysql using jdbc in Hibernate -->

<hibernate-configuration xmlns="http://www.hibernate.org/xsd/orm/cfg">
    <session-factory>
    <!-- Database connection settings -->
        <!-- 1. Database connection driver -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <!-- 2. Database URL -->
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/students</property>
        <!-- 3. Database username -->
        <property name="hibernate.connection.username">root</property>
        <!-- 4. Database password -->
        <property name="hibernate.connection.password">Ar@20051125</property>

        <!-- Command to create database tables if not created
            If we use create instead of update, it will try to create a table each time when run-->
        <property name="hibernate.hbm2ddl.auto">update</property>
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
    </session-factory>
</hibernate-configuration>